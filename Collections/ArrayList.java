package Collections;
public class ArrayList {
    /*
    ArrayList is best when we are required to retrieve element, and it is worse when we need to continuously add data(somewhere in the middle).
    deletion(somewhere in the middle):
     LinkedList is the best suitable for insertion and deletion but is worse for retrieval of data at constant basis.

     */
}
