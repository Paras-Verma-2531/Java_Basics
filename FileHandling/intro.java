package FileHandling;
public class intro {
    /*
    sometimes, we may require to store output  permanently for which file handling comes into picture.
    Classes we need to study in File Handling are::
    --------------------
    1.File
    2.FileWriter
    3.FileReader
    4.BufferedWriter
    5.BufferedReader
    6.PrintWriter
    ---------------------------------
    how to create a new file
    -------------------------------
    File f= new File("filename.txt");
    f.createNewFile();
    -------------------------------
    -------------------------------
    File class constructor::
    File f= new File("file name");->for current working directory
    File f= new File(String subdir,"file name");----> to  store at other directory
    File f= new File(file subdir,"file name");-----> in addition to 2nd constr, if a file is being referenced to that loc, use that file reference
---------------------------------------IMP METHODS IN FILE CLASS-----------------------------------
methods used in File
createNewFile();
.isFile();
.isDirectory();
makedir();
exists();
list();------->to list out all the files
length()--->returns the length of file(return long type)
f.delete();

     */
}
