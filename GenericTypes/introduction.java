package GenericTypes;

public class introduction {
    public static void main(String[] args) {
        /*
        Generics are type safe eg:(Arrays)-->only allows particular type but they are fixed in size
        main objective of generics are to provide type safety to the collections(Arraylist etc)..
        Generics provide type casting::
        Type safe::
        ArrayList al = new ArrayList(); before generic
        ArrayList<String> al = new ArrayList<String>(); After generic

         */
        System.out.println("this package contains all the work regarding generic classes and types");
    }
}
